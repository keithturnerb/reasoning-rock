import styled from 'styled-components'

const PaginationMenuWrapper = styled.div`
  width: 100%;
  background-color: transparent;
  color: #efffffff;
  margin-left: -1em; /* Parent Grid floating menu, so this is added to align menu */

  .pagination-menu-items {
    color: #efffffff;
    font-family: 'Amatic SC';
    font-size: 1em;
    font-style: normal;
    font-weight: bold;
  }
`

export default PaginationMenuWrapper
