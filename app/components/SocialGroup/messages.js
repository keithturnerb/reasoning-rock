/*
 * SocialGroup Messages
 *
 * This contains all the text for the SocialGroup component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.SocialGroup'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialGroup component!',
  },
})
